# Frontend Environment Variables

# The base URL for the backend API.
# During local development, this defaults to http://localhost:8000 if not set.
# In production, set this to the deployed backend URL (e.g., https://api.your-azure-domain.com).
VITE_API_BASE_URL=http://localhost:8000
